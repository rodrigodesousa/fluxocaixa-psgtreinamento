@model IEnumerable<Mvc_FluxoCaixa.Models.tipodelancamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descrição)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descrição)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipolancamentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipolancamentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipolancamentoId })
        </td>
    </tr>
}

</table>
